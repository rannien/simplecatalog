# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: wboyz.simplecatalog::lang.category.name
        searchable: true
    slug:
        label: wboyz.simplecatalog::lang.category.slug
        searchable: true